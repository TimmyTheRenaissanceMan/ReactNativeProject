import { CompactRestaurantInfo } from "../../../components/compactRestaurantInfo.component";

export const MapCallout = ({ restaurant }) => {
    return <CompactRestaurantInfo restaurant={restaurant} />;
};
